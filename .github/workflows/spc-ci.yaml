# on:
#   push:
#     branches:
#       - main
  

# jobs: 
#   Build:
#     runs-on: ubuntu-latest
#     steps: 
#       - name: checkout the code
#         uses: actions/checkout@v6
#       - name: Set up JDK
#         uses: actions/setup-java@v4
#         with:
#           java-version: '21' # Specify your Java version
#           distribution: 'temurin'
#           cache: 'maven'
#       - name: Buld maven clean package
#         run: mvn -B clean package --file pom.xml
#   Package:
#       runs-on: ubuntu-latest
#       needs: [Build]
#       steps:
#         - name: checkout the code
#           uses: actions/checkout@v6
#         - name: Upload Artifact
#           uses: actions/upload-artifact@v4
#           with:
#             name: my-app-jar
#             path: target/*.jar
#   Image-Build:
#     runs-on: ubuntu-latest
#     needs: [Build,Package]
#     steps: 
#     - name: checkout the code
#       uses: actions/checkout@v6
      
#     - name: Login to Docker Hub
#       uses: docker/login-action@v4
#       with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}
#     - name: Build the image
#       run: |
#            docker image build -t spc .
#            docker tag spc:latest ${{ secrets.DOCKER_USERNAME }}/spc:latest
#            docker push ${{ secrets.DOCKER_USERNAME }}/spc:latest
    
name: Spring Boot CI/CD

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:

      # Checkout source code
      - name: Checkout Code
        uses: actions/checkout@v4

      # Install Java
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      # Build Spring Boot jar
      - name: Build Maven Package
        run: mvn clean package -DskipTests

      # Verify jar exists
      - name: Verify Target Folder
        run: ls -la target

      # Login to Docker Hub
      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Build Docker Image
      - name: Build Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/spc:latest .

      # Push Docker Image
      - name: Push Docker Image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/spc:latest